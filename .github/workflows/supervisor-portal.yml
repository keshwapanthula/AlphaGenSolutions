name: Deploy Supervisor Portal

on:
  push:
    branches: [main]
    paths:
      - 'supervisor-portal/**'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: supervisor-portal

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: supervisor-portal/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_API_BASE_URL: https://alphagen-api.azurewebsites.net

      - name: Login to Azure (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Blob Static Website
        run: |
          az storage blob upload-batch \
            --account-name alphagensupervisor \
            --source dist/ \
            --destination '$web' \
            --overwrite \
            --auth-mode login

      - name: Print URL
        run: echo "Supervisor portal deployed to https://alphagensupervisor.z21.web.core.windows.net/"
